image: php:8.2

stages:
  - test
  - deploy

# 🧪 Tests - S'exécute sur toutes les branches
test:
  stage: test
  tags:
    - back
  before_script:
    # Installer les extensions PHP nécessaires si possible
    - sudo apt-get update -qq || echo "Pas de droits sudo, on continue..."
    - sudo apt-get install -y php8.2-dom php8.2-xml php8.2-zip php8.2-mbstring || echo "Installation des extensions échouée, on continue..."
    
    # Vérifier que PHP et Composer sont disponibles
    - php --version
    - composer --version || (echo "Composer non trouvé, installation..." && curl -sS https://getcomposer.org/installer | php && sudo mv composer.phar /usr/local/bin/composer)
    
    # Installation des dépendances PHP (ignorer les extensions manquantes)
    - composer install --prefer-dist --no-interaction --no-dev --ignore-platform-reqs --no-scripts
    
    # Générer l'autoload sans exécuter les scripts Laravel
    - composer dump-autoload --optimize --no-dev --no-scripts
    
    # Configuration de l'environnement de test
    - cp .env.testing .env
    - php artisan key:generate --force || echo "Erreur lors de la génération de clé, mais on continue..."
    
  script:
    - echo "🧪 Lancement des tests..."
    # Créer le dossier de logs si nécessaire
    - mkdir -p storage/logs
    - php artisan test --verbose
    - echo "✅ Tests terminés"
    
  artifacts:
    when: always
    reports:
      junit: storage/logs/junit.xml
    paths:
      - storage/logs/
    expire_in: 1 week

# 🚀 Déploiement - Seulement sur main et après succès des tests
deploy:
  stage: deploy
  tags:
    - back
  dependencies:
    - test
  script:
    - echo "🚚 Déploiement du back..."
    - sudo rm -rf /home/flowadmin/back/*
    - sudo cp -r . /home/flowadmin/back/
    - echo "🔃 Redémarrage de Nginx et PHP..."
    - sudo docker compose -f /home/flowadmin/docker-compose.yml restart nginx
    - echo "✅ Déploiement terminé"
  only:
    - main
  needs:
    - test

